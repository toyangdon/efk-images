language: java
arch:
  - arm64
os:
  - linux
services:
  - docker
branches:
  only:
    - master
before_install:
  - docker login -u "$DOCKER_USERNAME" -p $DOCKER_PASSWORD
script: 
  - export pwd=$PWD && echo $PWD
